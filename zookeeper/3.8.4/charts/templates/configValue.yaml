apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: zookeeper
    version: {{ .Chart.AppVersion | quote }}
data:
  zookeeper: |
{{ .Files.Get "files/zookeeperValue.yaml" | indent 4 }}
