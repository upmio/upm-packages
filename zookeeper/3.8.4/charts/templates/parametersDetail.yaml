apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: zookeeper
    version: {{ .Chart.AppVersion | quote }}
data:
  parameters.json: |
{{ .Files.Get "files/zookeeperParametersDetail.json" | indent 4 }}
