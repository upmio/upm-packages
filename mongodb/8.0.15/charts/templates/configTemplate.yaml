apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-template
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: mongodb
    version: {{ .Chart.AppVersion | quote }}
data:
  mongodb: |
{{ .Files.Get "files/mongodbTemplate.tpl" | indent 4 }}
