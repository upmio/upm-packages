apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: mongodb
    version: {{ .Chart.AppVersion | quote }}
data:
  mongodb: |
{{ .Files.Get "files/mongodbParametersDetail.json" | indent 4 }}
