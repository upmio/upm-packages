apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: mongodb
    version: {{ .Chart.AppVersion | quote }}
data:
  mongodb: |
{{ .Files.Get "files/mongodbValue.yaml" | indent 4 }}
