apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-template
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: redis
    version: {{ .Chart.AppVersion | quote }}
data:
  redis: |
{{ .Files.Get "files/redisTemplate.tpl" | indent 4 }}
