apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: redis
    version: {{ .Chart.AppVersion | quote }}
data:
  redis: |
{{ .Files.Get "files/redisValue.yaml" | indent 4 }}
