apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: milvus
    version: {{ .Chart.AppVersion | quote }}
data:
  milvus: |
{{ .Files.Get "files/milvusValue.yaml" | indent 4 }}
