apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-template
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: milvus
    version: {{ .Chart.AppVersion | quote }}
data:
  milvus: |
{{ .Files.Get "files/milvusTemplate.tpl" | indent 4 }}
