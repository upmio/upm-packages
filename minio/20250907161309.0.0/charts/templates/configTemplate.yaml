apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-template
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: minio
    version: {{ .Chart.AppVersion | quote }}
data:
  minio: |
{{ .Files.Get "files/minioTemplate.tpl" | indent 4 }}
