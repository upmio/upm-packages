apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: kafka
    version: {{ .Chart.AppVersion | quote }}
data:
  kafka: |
{{ .Files.Get "files/kafkaParametersDetail.json" | indent 4 }}
