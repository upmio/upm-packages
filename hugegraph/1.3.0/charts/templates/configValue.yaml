apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: hugegraph
    version: {{ .Chart.AppVersion | quote }}
data:
  hugegraph: |
{{ .Files.Get "files/hugegraphValue.yaml" | indent 4 }}
