apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: hugegraph
    version: {{ .Chart.AppVersion | quote }}
data:
  hugegraph: |
{{ .Files.Get "files/hugegraphParametersDetail.json" | indent 4 }}
