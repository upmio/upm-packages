apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: etcd
    version: {{ .Chart.AppVersion | quote }}
data:
  etcd: |
{{ .Files.Get "files/etcdParametersDetail.json" | indent 4 }}
