apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: etcd
    version: {{ .Chart.AppVersion | quote }}
data:
  etcd: |
{{ .Files.Get "files/etcdValue.yaml" | indent 4 }}
