apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-parameters-detail
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: hugegraph-hubble
    version: {{ .Chart.AppVersion | quote }}
data:
  hugegraph-hubble: |
{{ .Files.Get "files/hugegraphHubbleParametersDetail.json" | indent 4 }}
