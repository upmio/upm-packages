apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-value
  namespace: {{ .Release.Namespace | quote }}
  labels:
    upm.io/owner: upm
    type: hugegraph-hubble
    version: {{ .Chart.AppVersion | quote }}
data:
  hugegraph-hubble: |
{{ .Files.Get "files/hugegraphHubbleValue.yaml" | indent 4 }}
